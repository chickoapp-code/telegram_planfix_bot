# Инструкция по остановке бота на сервере

Если вы удалили папку, но бот продолжает работать, значит он запущен как фоновый процесс.

## 🔍 Шаг 1: Найти процесс бота

### Вариант 1: Использовать скрипт
```bash
bash find_bot_process.sh
```

### Вариант 2: Вручную
```bash
# Найти процессы Python
ps aux | grep python

# Найти процессы с main.py
ps aux | grep main.py

# Найти все процессы с "bot"
ps aux | grep bot
```

## 🛑 Шаг 2: Остановить процесс

### Вариант 1: Использовать скрипт
```bash
bash stop_bot.sh
```

### Вариант 2: Вручную
```bash
# Остановить процесс по PID (номер из первого столбца)
kill <PID>

# Если не останавливается, принудительно
kill -9 <PID>
```

## 🔍 Шаг 3: Проверить сервисы и менеджеры процессов

### Проверить systemd (если бот запущен как сервис)
```bash
bash check_bot_service.sh
```

Или вручную:
```bash
# Найти сервисы
systemctl list-units --type=service --all | grep bot

# Остановить сервис
sudo systemctl stop <service_name>

# Отключить автозапуск
sudo systemctl disable <service_name>
```

### Проверить supervisor
```bash
# Посмотреть процессы
supervisorctl status

# Остановить процесс
supervisorctl stop <process_name>
```

### Проверить screen
```bash
# Посмотреть сессии
screen -ls

# Войти в сессию
screen -r <session_name>
# Затем нажать Ctrl+C для остановки

# Или убить сессию
screen -S <session_name> -X quit
```

### Проверить tmux
```bash
# Посмотреть сессии
tmux ls

# Убить сессию
tmux kill-session -t <session_name>
```

### Проверить nohup
```bash
# Найти процессы nohup
ps aux | grep nohup

# Остановить как обычный процесс
kill <PID>
```

## 📋 Полная последовательность действий

1. **Найти процесс:**
   ```bash
   ps aux | grep -E "(main\.py|python.*main)" | grep -v grep
   ```

2. **Остановить процесс:**
   ```bash
   kill <PID>
   ```

3. **Проверить, что процесс остановлен:**
   ```bash
   ps aux | grep -E "(main\.py|python.*main)" | grep -v grep
   ```
   (не должно быть вывода)

4. **Если процесс не останавливается:**
   ```bash
   kill -9 <PID>
   ```

5. **Проверить сервисы:**
   ```bash
   systemctl list-units --type=service --all | grep bot
   supervisorctl status
   screen -ls
   tmux ls
   ```

## ⚠️ Важно

После остановки бота:
- Убедитесь, что процесс действительно остановлен
- Проверьте, что автозапуск отключен (если использовался systemd/supervisor)
- Удалите старую папку (если еще не удалили)
- Загрузите новую версию кода
- Запустите бота заново

## 🔄 После остановки

Когда бот остановлен, вы можете:
1. Удалить старую папку (если еще не удалили)
2. Загрузить новую версию кода
3. Установить зависимости
4. Настроить .env файл
5. Запустить бота заново

