#!/bin/bash
# Скрипт запуска бота
# Использование: ./start_bot.sh [polling|webhook|both]

set -e

# Получаем директорию, где находится скрипт
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Определяем режим работы (по умолчанию both)
MODE="${1:-both}"

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение не найдено. Создайте его командой: python3 -m venv venv"
    exit 1
fi

# Активируем виртуальное окружение
source venv/bin/activate

# Проверяем наличие .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден. Убедитесь, что он существует и содержит необходимые настройки."
    exit 1
fi

# Устанавливаем переменную окружения для systemd
export SYSTEMD_SERVICE=1

# Запускаем бота
echo "🚀 Запуск бота в режиме: $MODE"
python3 run.py --mode "$MODE"




