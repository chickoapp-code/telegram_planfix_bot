# Быстрый старт: Напоминания о невзятых задачах

## Что это?

Автоматические напоминания исполнителям о задачах, которые еще не взяты в работу. Каждый час Planfix отправляет вебхук боту, и бот повторно уведомляет всех ответственных исполнителей.

## Настройка в Planfix (5 минут)

1. **Создайте сценарий**:
   - Настройки → Автоматизация → Сценарии → Создать сценарий

2. **Условия**:
   - Триггер: Задача создана или обновлена
   - Условие: Статус = "Новая" И назначенных исполнителей нет
   - Расписание: Каждый час

3. **Действие - HTTP запрос**:
   - **URL**: `http://crmbot.restme.pro/planfix/webhook`
   - **Метод**: `POST`
   - **Content-Type**: `application/json`
   - **Тело запроса** (скопируйте и вставьте):

```json
{
  "event": "task.reminder",
  "task": {
    "id": "{{Задача.Идентификатор}}"
  }
}
```

**Важно**: Используйте русские переменные Planfix:
- `{{Задача.Идентификатор}}` - для ID задачи

4. **Сохраните сценарий** и активируйте его.

## Готово! 

Теперь каждый час бот будет проверять невзятые задачи и отправлять напоминания исполнителям.

## Проверка работы

Проверьте логи бота:
```bash
tail -f logs/bot.log | grep "Reminder"
```

Вы должны увидеть сообщения вида:
```
🔔 Reminder for unassigned task 12345 - resending notifications to executors
```

## Дополнительная информация

Подробная документация: `PLANFIX_REMINDER_SETUP.md`

